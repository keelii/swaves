[package]
name = "swaves-supervisor"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Supervisor runtime protocol and master/worker control APIs for swaves"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
thiserror = "2.0.18"

[dev-dependencies]
tempfile = "3.20.0"
