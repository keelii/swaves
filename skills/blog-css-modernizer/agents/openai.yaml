interface:
  display_name: "Blog CSS Expert"
  short_description: "Split site/admin styling with Tufte + Oat alignment"
  default_prompt: "Use $blog-css-modernizer to implement minimal, modern CSS updates for either site (Tufte-based) or admin (Oat-based) while keeping existing style consistency."
